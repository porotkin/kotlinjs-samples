Nested JsPlainObject interface leads to an error

1. Run

```shell
./gradlew build
```

2. Expected result: Test passes
2. Alternative result: Test fails with `ReferenceError: navigationHelpButton is not defined`
